<% if flash[:messages]  %>
 <% end %>
<h1> Make a new appearance </h1>
<li>
<%= form_for @appearance do |f|%>
<%= f.label :guest %>
<%= f.collection_select :guest_id, @guests, :id, :name %>
<%= f.label :episode %>
<%= f.collection_select :episode_id, @episodes, :id, :name %>
<%= f.label :rating %>
<%= f.text_field :rating %>
<%= f.submit %>
<%end%> </li>